pipeline {
  agent { dockerfile true }
  stages {
    stage ('Test') {
      steps {
        sh '''
          curl --version
        '''
      }
    }
    stage ('Test') {
            steps {
                echo 'Hello, JDK'
            }
        }
    }
}
